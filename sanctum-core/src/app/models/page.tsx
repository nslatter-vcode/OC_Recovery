export default function ModelsPage() {
  return (
    <main>
      <h1 className="h1">Models</h1>
      <p className="subtle">Placeholder. Next: available models, which are missing, defaults, and where each is used (agent/cron/heartbeat).</p>
    </main>
  );
}
